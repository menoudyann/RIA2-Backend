eureka:
  instance:
    hostname: localhost
  client:
    service-url:
        defaultZone: http://${eureka.instance.hostname}:8761/eureka/
spring:
  application:
    name: dataobject
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
server:
  port: 8070
